//prints factorial
fn main(string args) returns int {
  int i = 0;
  while(i < 4){
    int f = factorial(i);
    print("%d",f);
    step i, 1; 
  }  
  return 1;
}
fn factorial(n: int) returns int {
    if n < 1 then return 1;
    return n * factorial(n - 1);
}
